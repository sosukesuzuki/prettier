longfunctionWithCallBack(
  "blabla",
  (thing: string): complex<type<something>> => {
    code();
  },
  foobarbazblablabla
);

loooong("/", 3, (): a | b | c => {});

loooong("/", 3, (): complex<type<something>> => {});

loooong("/", looooooooooooooooooooooooooong, (): complex<type<something>> => {});

longfunctionWithCallBack(
  "blabla",
  foobarbazblablabla,
  (thing: string): complex<type<something>> => {
    code();
  }
);

longfunctionWithCallBack(
  "blabla",
  foobarbazblablabla,
  (thing: string): Foooo | Barrrrrr | BAAAZ => {
    code();
  }
);

longfunctionWithCallBack(
  "blabla",
  foobarbazblablabla,
  (thing: string): Fooooooooooooooooooooooo => {
    code();
  }
);
